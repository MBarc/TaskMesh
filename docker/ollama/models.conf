# Ollama Model Build Variants
# Format: RECOMMENDED_RAM  OLLAMA_MODEL
#
# RECOMMENDED_RAM: minimum RAM suggested for running this model (used in image tag)
# OLLAMA_MODEL:    model name exactly as Ollama expects it
#
# To add a new variant, simply add a new line below.
# Then run: ./ollama/build.sh

8gb     qwen2.5:3b
16gb    qwen2.5:7b
